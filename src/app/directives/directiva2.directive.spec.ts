import { Directiva2Directive } from './directiva2.directive';

describe('Directiva2Directive', () => {
  it('should create an instance', () => {
    const directive = new Directiva2Directive();
    expect(directive).toBeTruthy();
  });
});
