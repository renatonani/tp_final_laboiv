import { Directiva3Directive } from './directiva3.directive';

describe('Directiva3Directive', () => {
  it('should create an instance', () => {
    const directive = new Directiva3Directive();
    expect(directive).toBeTruthy();
  });
});
